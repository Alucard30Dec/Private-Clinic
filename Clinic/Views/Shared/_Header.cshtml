@{
    var isPatient = Request.IsAuthenticated && User.IsInRole("Patient");
}

<section class="navbar navbar-default navbar-static-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon icon-bar"></span>
                <span class="icon icon-bar"></span>
                <span class="icon icon-bar"></span>
            </button>

            <a href="@Url.Action("Index","Home")" class="navbar-brand">
                <i class="fa fa-h-square"></i>ealth Center
            </a>
        </div>

        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav nav-left">

                @* Show Home and About for guests, with smoothScroll *@
                @if (!isPatient)
                {
                    @* === CORRECTED LINK TARGET HERE === *@
                    <li><a href="@Url.Action("Index","Home")#home" class="smoothScroll">Home</a></li>
                    <li><a href="@Url.Action("Index","Home")#about" class="smoothScroll">About Us</a></li>
                }

                @* Always show Doctors link *@
                <li>@Html.ActionLink("Doctors", "Index", "Doctors")</li>

                @* Show History (for Patient) or Make an appointment (for Guest) *@
                @if (isPatient)
                {
                    <li>@Html.ActionLink("History", "Index", "Bookings")</li>
                }
                else
                {
                    <li>@Html.ActionLink("Make an appointment", "Index", "Appointment")</li>
                }
            </ul>

            <ul class="nav navbar-nav navbar-right nav-right">
                @Html.Partial("_LoginPartial") @* Renders Login/Register or User dropdown *@
            </ul>
        </div>
    </div>
</section>