@{
    Layout = null;
    var title = ViewBag.Title as string ?? "Private Clinic";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>@title</title>
    <meta name="description" content="Private Clinic - Đặt lịch khám trực tuyến nhanh chóng, phong cách giống Ivy Health." />

    <!-- Vendor CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
          rel="stylesheet" />

    <!-- App CSS (sau vendor để ghi đè) -->
    @Styles.Render("~/Content/appcss")

    @RenderSection("Styles", required: false)
    @* <link rel="icon" type="image/png" href="~/favicon.png" /> *@
</head>
<body id="top" class="d-flex flex-column min-vh-100 @ViewBag.PageClass">
    <!-- Header -->
    @Html.Partial("_Header")

    <!-- Alerts từ TempData -->
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success text-center mb-0 rounded-0">@TempData["Success"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger text-center mb-0 rounded-0">@TempData["Error"]</div>
    }

    <!-- Body -->
    <main class="flex-grow-1 pt-5">
        @RenderBody()
    </main>

    <!-- Footer -->
    @Html.Partial("_Footer")

    <!-- Vendor JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>

    <!-- App JS (đã tách ra bundle) -->
    @Scripts.Render("~/bundles/appjs")

    <!-- LOGIN MODAL -->
    <div class="modal fade" id="loginModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-md" role="document">
            <div class="modal-content modal-auth border-0 rounded-4 overflow-hidden">
                <div class="modal-header border-0 justify-content-center position-relative">
                    <h5 class="modal-title auth-title fw-800 text-center">Đăng nhập Private Clinic</h5>
                    <button type="button" class="btn-close position-absolute end-0 me-3"
                            data-bs-dismiss="modal" aria-label="Đóng"></button>
                </div>
                <div class="modal-body pt-0">
                    <form method="post" action="@Url.Action("Login","Account")" id="login-form" novalidate>
                        @Html.AntiForgeryToken()
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Tên Đăng Nhập</label>
                            <div class="input-group input-group-lg auth-input">
                                <span class="input-group-text"><i class="bi bi-person"></i></span>
                                <input type="text" name="UserNameOrEmail" class="form-control" required />
                            </div>
                        </div>
                        <div class="mb-1">
                            <label class="form-label fw-semibold">Mật Khẩu</label>
                            <div class="input-group input-group-lg auth-input">
                                <span class="input-group-text"><i class="bi bi-lock"></i></span>
                                <input type="password" name="Password" class="form-control" required />
                                <button type="button" class="btn toggle-pass" aria-label="Hiện/ẩn mật khẩu">
                                    <i class="bi bi-eye"></i>
                                </button>
                            </div>
                        </div>
                        <div class="d-flex justify-content-between align-items-center mb-3 small">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" value="true" id="RememberMe" name="RememberMe">
                                <label class="form-check-label" for="RememberMe">Ghi nhớ tôi</label>
                            </div>
                            <a href="#" class="link-offset-2 js-open-forgot">Quên mật khẩu?</a>
                        </div>
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-ivy-cta btn-lg">Đăng nhập</button>
                            <a class="btn btn-outline-secondary btn-lg" href="@Url.Action("Register","Account")">Đăng ký</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- FORGOT PASSWORD MODAL -->
    <div class="modal fade" id="forgotModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-md">
            <div class="modal-content modal-auth border-0 rounded-4">
                <div class="modal-header border-0 justify-content-center position-relative">
                    <h5 class="modal-title fw-800 text-center text-teal">Quên mật khẩu</h5>
                    <button type="button" class="btn-close position-absolute end-0 me-3"
                            data-bs-dismiss="modal" aria-label="Đóng"></button>
                </div>
                <div class="modal-body pt-0">
                    <form method="post" action="@Url.Action("ForgotPassword","Account")" id="forgot-form" novalidate>
                        @Html.AntiForgeryToken()
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Tên đăng nhập</label>
                            <div class="input-group input-group-lg auth-input">
                                <span class="input-group-text"><i class="bi bi-person"></i></span>
                                <input type="text" name="UserName" class="form-control" placeholder="Nhập tên đăng nhập" required />
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Mật khẩu mới</label>
                            <div class="input-group input-group-lg auth-input">
                                <span class="input-group-text"><i class="bi bi-lock"></i></span>
                                <input type="password" name="NewPassword" class="form-control" placeholder="Mật khẩu mới" required />
                                <button type="button" class="btn toggle-pass" aria-label="Hiện/ẩn mật khẩu">
                                    <i class="bi bi-eye"></i>
                                </button>
                            </div>
                        </div>
                        <div class="mb-4">
                            <label class="form-label fw-semibold">Xác nhận mật khẩu mới</label>
                            <div class="input-group input-group-lg auth-input">
                                <span class="input-group-text"><i class="bi bi-lock"></i></span>
                                <input type="password" name="ConfirmPassword" class="form-control" placeholder="Nhập lại mật khẩu mới" required />
                                <button type="button" class="btn toggle-pass" aria-label="Hiện/ẩn mật khẩu">
                                    <i class="bi bi-eye"></i>
                                </button>
                            </div>
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-ivy-cta btn-lg">Xác nhận</button>
                        </div>
                    </form>
                </div>
                <div class="modal-footer border-0 small text-muted justify-content-center">
                    Vui lòng giữ an toàn mật khẩu của bạn.
                </div>
            </div>
        </div>
    </div>

    @RenderSection("Scripts", required: false)
</body>
</html>
